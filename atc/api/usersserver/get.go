package usersserver

import (
	"net/http"
)

func (s *Server) GetUsersSince(w http.ResponseWriter, r *http.Request) {
	//days, err := strconv.Atoi(r.FormValue("since"))
	//if err != nil {
	//	//
	//}
	// s.userFactory.GetAllUsersByLoginDate()
	return
}
